<Page x:Class="BigMountainX.TicketingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:BigMountainX"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Background="#FF6A6A6A">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <Button x:Name="btn_ticket"
                    Content="Buy Standard Ticket"
                    Margin="5"
                    Click="OnStandardTicket"
                    HorizontalAlignment="Stretch" />
            <Button x:Name="btn_vipticket"
                    Content="Buy Table (VIP Ticket)"
                    Margin="5"
                    Click="OnVIPTicket"
                    HorizontalAlignment="Stretch" />
            <Button x:Name="btn_openmic"
                    Content="Buy Open Mic Ticket"
                    Margin="5"
                    Click="OnOpenMicTicket"
                    HorizontalAlignment="Stretch" />
        </StackPanel>
    </Grid>
</Page>
